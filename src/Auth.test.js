import React from 'react';
import { screen } from '@testing-library/react';
import { render } from '../src/test-utils';
// import App from './App';
import Auth from '../src/shared/pages/Auth';

test('returns a user not found error', async () => {
    // const email = 'notFound@email.com';
    // const password = '123456';
    // expect.assertions(1);
    //   try {
    //     await Auth.signInHandler(email, password) 
    //   } catch (e) {
    //     expect(e).toMatch('A user with this email address does not exist. Please create an account.');
    //   }
});
